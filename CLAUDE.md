# Project instructions

## Previewing / running the app

- **Do NOT start the dev server or run the browser preview to verify changes.** The user previews the app themselves. Make the code change, do a static check (syntax, template references, stale variables), and report what to look at — don't launch `runserver` or the preview tools.

[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = tests.py test_*.py *_tests.py
# E2E tests run headless Chromium against Django's live server.
addopts = --browser chromium
markers =
    e2e: browser-driven end-to-end tests (Playwright)
